import { Title } from "@angular/platform-browser";

export class Graph {
    title: string;
    type: string;
    data: any;
    columnNames: any;
    options: any;
    width: number;
    height: number;
}